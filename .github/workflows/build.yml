name: Build

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
    - name: Checkout Source
      uses: actions/checkout@v4
      
    - name: Setup Rust
      uses: dtolnay/rust-toolchain@stable
      
    - name: Cache Cargo
      uses: actions/cache@v4
      with:
        path: |
          ~/.cargo/registry
          ~/.cargo/git
          target
        key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
        restore-keys: |
          ${{ runner.os }}-cargo-
  
    - name: Build Release
      run: cargo build --release
      
    - name: Create Release Archive
      run: |
        mkdir -p release
        cp target/release/bigly release/
        cd release
        tar -czf bigly-${{ runner.os }}.tar.gz bigly
      
    - name: Upload Build Artifact
      uses: actions/upload-artifact@v4
      with:
        name: bigly-${{ runner.os }}
        path: release/bigly-${{ runner.os }}.tar.gz
        retention-days: 7
    
    - name: Create Release
      if: github.ref == 'refs/heads/main'
      uses: softprops/action-gh-release@v2
      with:
        tag_name: latest
        name: Latest Build
        body: Automated build from main branch
        draft: false
        prerelease: true
        files: release/bigly-${{ runner.os }}.tar.gz
        token: ${{ secrets.GITHUB_TOKEN }}
